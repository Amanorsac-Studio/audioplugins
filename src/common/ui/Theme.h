#pragma once

#include <juce_gui_basics/juce_gui_basics.h>

namespace amanorsac::theme
{
inline const auto digitalBackground = juce::Colour::fromRGB(3, 7, 12);
inline const auto digitalPanel = juce::Colour::fromRGB(8, 16, 26);
inline const auto digitalBorder = juce::Colour::fromRGB(34, 70, 95);
inline const auto electricBlue = juce::Colour::fromRGB(46, 126, 255);
inline const auto analogBackground = juce::Colour::fromRGB(15, 13, 11);
inline const auto analogMetal = juce::Colour::fromRGB(24, 27, 25);
inline const auto brass = juce::Colour::fromRGB(216, 157, 63);
inline const auto cream = juce::Colour::fromRGB(240, 229, 200);
inline const auto primaryText = juce::Colour::fromRGB(244, 247, 250);
inline const auto secondaryText = juce::Colour::fromRGB(169, 179, 194);
}

